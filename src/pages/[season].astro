---
import { SEASONS } from "../data";

export function getStaticPaths() {
  return SEASONS.map((season) => ({
    params: { season: season.id },
  }));
}

// TODO Fill in archive page
// TODO meta tags
return Astro.redirect("/");
---
