---
import type { HTMLAttributes } from "astro/types";
type Props = HTMLAttributes<"a">;
const { class: classStr, href, ...attrs } = Astro.props;
---

<style>
  @reference "../styles/global.css";

  .NBASTT-Link.active {
    @apply pointer-events-none cursor-default text-indigo-400 dark:text-indigo-100;
  }
</style>

<a
  class:list={[
    "NBASTT-Link",
    "text-purple-600 underline hover:text-indigo-100 dark:text-indigo-400",
    classStr,
  ]}
  href={href}
  {...attrs}><slot /></a
>
